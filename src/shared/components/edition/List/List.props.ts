export interface ListProps {
    children: React.ReactNode;
    ordered?: boolean;
    className?: string;
}

export interface ListItemProps {
    children: React.ReactNode;
    className?: string;
}

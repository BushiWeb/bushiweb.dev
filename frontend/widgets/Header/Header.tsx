import Logo from 'shared/components/Logo';
import './Header.css';

/* Global UI header */
export default function Header() {
    return (
        <header className="header">
            <Logo />
        </header>
    );
}
